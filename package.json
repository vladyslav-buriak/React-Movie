{
    "name": "movies-recommendation",
    "version": "1.0.0",
    "description": "app about recommends movies",
    "main": "./back/src/index",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "start":"node back/src/index.js",
      "build":"cd front && yarn run build",
      "install":"cd front  && yarn install && cd ../back && yarn install"
    },
    "keywords": [
      "example",
      "heroku"
    ],
    "author": "Buriak Vladyslav",
    "license": "MIT",
    "dependencies": {
      "express": "^4.9.8"
    },
    "engines": {
      "node": "18.x"
    }
  }

