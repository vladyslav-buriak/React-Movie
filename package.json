{
    "name": "movies-recommendation",
    "version": "1.0.0",
    "description": "app about recommends movies",
    "main": "./back/src/index",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "start":"node ./back/src/index",
      "build":"cd front && npm run build",
      "install":"cd front && npm install --legacy-peer-deeps  && cd ../back && npm install"
    },
    "keywords": [
      "example",
      "heroku"
    ],
    "author": "Buriak Vladyslav",
    "license": "MIT",
    "dependencies": {
      "express": "^4.9.8"
    },
    "engines": {
      "node": "18.x"
    }
  }

